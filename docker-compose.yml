name: signalfoundry

services:
  timescaledb:
    image: ${TIMESCALEDB_IMAGE:-timescale/timescaledb:2.27.1-pg18}
    container_name: signalfoundry-timescaledb
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-signalfoundry}
      POSTGRES_USER: ${POSTGRES_USER:-signalfoundry}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-signalfoundry_dev_password}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-35433}:5432"
    volumes:
      - signalfoundry_timescaledb_data:/var/lib/postgresql/data
      - ./sqls:/docker-entrypoint-initdb.d:ro
    healthcheck:
      # The image starts a temporary Unix-socket-only PostgreSQL server while
      # running /docker-entrypoint-initdb.d. Requiring TCP keeps Compose from
      # reporting healthy before the final database server is accepting the
      # same connections used by Prisma and the application.
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  redis:
    image: ${REDIS_IMAGE:-redis:8-alpine}
    container_name: signalfoundry-redis
    restart: unless-stopped
    command:
      - redis-server
      - --appendonly
      - "yes"
      - --save
      - "60"
      - "1000"
    ports:
      - "127.0.0.1:${REDIS_PORT:-36380}:6379"
    volumes:
      - signalfoundry_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  clickhouse:
    image: ${CLICKHOUSE_IMAGE:-clickhouse/clickhouse-server:25.8}
    container_name: signalfoundry-clickhouse
    restart: unless-stopped
    environment:
      CLICKHOUSE_DB: ${CLICKHOUSE_DB:-signalfoundry}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER:-signalfoundry}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-signalfoundry_dev_password}
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: "1"
    ports:
      - "127.0.0.1:${CLICKHOUSE_HTTP_PORT:-38123}:8123"
      - "127.0.0.1:${CLICKHOUSE_NATIVE_PORT:-39023}:9000"
    volumes:
      - signalfoundry_clickhouse_data:/var/lib/clickhouse
      - signalfoundry_clickhouse_logs:/var/log/clickhouse-server
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "clickhouse-client --host 127.0.0.1 --user \"$${CLICKHOUSE_USER}\" --password \"$${CLICKHOUSE_PASSWORD}\" --query 'SELECT 1'",
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  loki:
    image: ${LOKI_IMAGE:-grafana/loki:3.6.0}
    container_name: signalfoundry-loki
    restart: unless-stopped
    user: "0:0"
    command:
      - -config.file=/etc/loki/config.yaml
    ports:
      - "127.0.0.1:${LOKI_PORT:-33100}:3100"
    volumes:
      - ./deploy/observability/loki/loki-config.yaml:/etc/loki/config.yaml:ro
      - signalfoundry_loki_data:/loki

  grafana:
    image: ${GRAFANA_IMAGE:-grafana/grafana:13.0.1}
    container_name: signalfoundry-grafana
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-signalfoundry_dev_password}
      GF_USERS_DEFAULT_THEME: light
      GF_AUTH_ANONYMOUS_ENABLED: ${GRAFANA_ANONYMOUS_ENABLED:-true}
      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
    ports:
      - "127.0.0.1:${GRAFANA_PORT:-33012}:3000"
    volumes:
      - ./deploy/observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - signalfoundry_grafana_data:/var/lib/grafana
    depends_on:
      - loki

  alloy:
    image: ${GRAFANA_ALLOY_IMAGE:-grafana/alloy:v1.16.1}
    container_name: signalfoundry-alloy
    restart: unless-stopped
    command:
      - run
      - /etc/alloy/config.alloy
      - --server.http.listen-addr=0.0.0.0:12345
      - --storage.path=/var/lib/alloy/data
    ports:
      - "127.0.0.1:${GRAFANA_ALLOY_PORT:-33145}:12345"
    volumes:
      - ./deploy/observability/alloy/config.alloy:/etc/alloy/config.alloy:ro
      - ./tmp:/var/log/signalfoundry/tmp:ro
      - ./.next:/var/log/signalfoundry/.next:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - signalfoundry_alloy_data:/var/lib/alloy
    depends_on:
      - loki

volumes:
  signalfoundry_timescaledb_data:
  signalfoundry_redis_data:
  signalfoundry_clickhouse_data:
  signalfoundry_clickhouse_logs:
  signalfoundry_loki_data:
  signalfoundry_grafana_data:
  signalfoundry_alloy_data:
